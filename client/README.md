# Client UI

This directory will contain the front-end mobile application.

The UI will connect to the API and will be built separately.

All game logic resides in the API - the UI is purely presentational.

